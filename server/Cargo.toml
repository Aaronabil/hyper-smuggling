[package]
name = "hyper-server"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14.20", features = ["full"] }  # Vulnerable version
tokio = { version = "1", features = ["full"] }